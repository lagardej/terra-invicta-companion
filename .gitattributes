# Normalize text files and keep LF in the repository
* text=auto eol=lf

# Windows scripts should use CRLF on checkout
*.bat text eol=crlf
*.cmd text eol=crlf

# Unix scripts should use LF
*.sh text eol=lf
